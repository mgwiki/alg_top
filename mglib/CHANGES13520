## Changes in bck13520 (vs bck13519)

Fixed compilation errors in stereo_S_inv_fn_in_Sn2 and stereo_S_inv_fn_not_south:

1. stereo_S_inv_fn_in_Sn2:
   - Fixed HinvFnE3 proof using `prove` tactic + `apply (product_space_graphI ...)`
   - Key pattern: use `prove graph 3 g :e product_space 3 Xi` to unfold def
   - Then apply product_space_graphI, use ordsuccE for cases
   - In each case: `rewrite Hjeq` then `prove (if n=0 then ... else ...) :e R` for beta-reduction
   - HnormSq1 still admitted (norm = 1 computation to be done)
   - Overall lemma still Admitted (depends on HnormSq1)

2. stereo_S_inv_fn_not_south (now Qed!):
   - Fixed Hfinv2 proof using `prove apply_fun (graph 3 g) 2 = ...` for definition unfolding
   - Fixed H2d0 using correct eq direction: Heq (fun _ b => b = ...) (eq_symm ...)
   - Fixed Hcoord2 using Heq (fun z _ => apply_fun (stereo_S_inv_fn u) 2 = apply_fun z 2)
   - Fixed Hfinv2_ne_m1: used Hcoord2 (fun _ b => b = minus_SNo 1) Hsp2
   - Fixed neq proofs: neq_ordsucc_0 1 for 2≠0, In_irref 1 for 2≠1

Key insight: `prove` tactic is essential for:
- Unfolding definitions like stereo_S_inv_fn to their graph form
- Beta-reducing `(fun i => ...) n` to `if n=0 then ...` form

File compiles cleanly. Line count: ~403961 lines.
